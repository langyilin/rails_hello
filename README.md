# `rails_hello`: An example image for use with faradayio/conductor

This is a simple Rails application that we build into a Docker image.
See [conductor](https://github.com/faradayio/conductor) for more details.
